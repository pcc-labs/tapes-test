# Copy to .env beside this file. docker compose reads it automatically, and
# run.sh reads it for its own preflight check. A key already exported in the
# shell wins over this file.
OPENAI_API_KEY=sk-...
